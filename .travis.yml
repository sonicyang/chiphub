# https://travis-ci.org/tornadoweb/tornado
language: python
python:
    - 3.4
    - 3.5

env:
    - DEV=true
    - DEV=false

install:
    - pip install -r requirements.txt

script:
    - echo "No Test for Now"

notifications:
      slack: chiphub:swn2THUyBtrnozt5Hlo2knDW
